# Create /var/volatile/tmp so the /var/tmp -> volatile/tmp symlink resolves.
# Without this, any service using PrivateTmp=yes fails on a read-only rootfs
# because systemd cannot set up the private /var/tmp bind mount.
d /var/volatile/tmp 1777 root root -
