# Podman engine configuration for appliance-os.
# See containers.conf(5) for all options.

[engine]
# Use persistent storage for temporary image content during operations
# like `podman load` and `podman pull`. The default /var/tmp lives on
# the read-only rootfs which has no space for container image layers.
image_copy_tmp_dir = "/data/platform/containers/tmp"
