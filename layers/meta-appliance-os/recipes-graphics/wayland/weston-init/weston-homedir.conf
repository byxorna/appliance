# Ensure /home/weston exists on the /home partition.
# The weston-init recipe creates it in the rootfs at build time, but
# /home is a separate partition that may not include it after an OTA
# update or if initial image population via WKS failed.
d /home/weston 0755 weston weston -
