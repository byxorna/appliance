# Persistent config directory for Feishin.
# Creates the mount point under the kiosk user's home and the
# source directory on the persistent /data partition.
d /home/kiosk/.config 0750 kiosk kiosk -
d /home/kiosk/.config/feishin 0750 kiosk kiosk -
